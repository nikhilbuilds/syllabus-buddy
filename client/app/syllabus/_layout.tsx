import { Stack } from "expo-router";

export default function SyllabusLayout() {
  return (
    <Stack
      screenOptions={{
        headerStyle: {
          backgroundColor: "#25292e",
        },
        headerTintColor: "#fff",
      }}
    />
  );
}
